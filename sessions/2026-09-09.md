# Session — 2026-09-09

Working session covering: story generation continuation, parallel-run tooling, suite dashboard,
client reference-material parity audit, and a parked cloud/CI execution decision.

## Where we are (state)

- **Latest commit:** `2ee1888` on `origin/master` (all work below is committed + pushed).
- **Environment:** QA (`https://outsystems-qa.asteronlife.co.nz`). Edge config, gitignored creds.
- **Accounts:** **10 QA accounts wired** in gitignored `apps/asteron-quote-apply/accounts.json`
  (ids a–j = tom.good+good/+2/+3..+9 and sam.wilson+12345; all `Rabbit@431` except sam = `P@ssw0rd135`).
  Map to `.auth/state-qa-<id>.json` (auto-created on first login).
- **Parallelism proven:** `node tools/parallel-run.js --scaling-test --streams 10` = **10/10 clean,
  0 casualties, ~3.9 min, peak 13.7/17 GB**. Framework scales to 10; ceiling is host RAM + the
  one-session-per-account limit (max 10 concurrent = 10 accounts).
- **Dashboard:** auto-updates after every run (reporter emits `summary.json` + rebuilds
  `test-runs/DASHBOARD.md` + `dashboard.html`). Manual rebuild: `node tools/build-dashboard.js`.
  Current totals ~40 specs / ~346 tests / ~82% pass (mix of QA + legacy dev runs; will normalise
  after a full QA `--all` run).

## Tooling added this session (all committed)

- `tools/parallel-run.js` — N-account launcher (reads accounts.json; `--all` / named specs /
  `--scaling-test` / `--list`; `KILL_STRAY_EDGE=false` baked in; auto-scaled stagger). Docs in
  `.kiro/steering/project-context.md` ("Parallel test runs").
- `tools/build-dashboard.js` + reporter `summary.json` emission — suite dashboard.
- `reference/` folder for client materials (bulky raw exports gitignored; only README/SOURCE/
  analysis/audit .md committed).
- `reference/parity-audit.md` — crosswalk of our specs vs the client's manual suite (see below).
- `.kiro/steering/reference-reconciliation.md` — reusable process for mining any client/manual
  suite (wired into TEST-GENERATION-PROCESS.md).
- `docs/cloud-execution.md` + `.github/workflows/nightly-suite.yml` — **PARKED** (see below).

## Client parity audit — key outcomes (reference/parity-audit.md)

Client materials in `reference/client-manual-test-suite/OneDrive_2/3/4` (2017→2026). **Treated as
DATED LEADS, not truth** (per the staleness caveat). Findings bucketed:
- **DURABLE (act now):** bundling negative/boundary sub-cases, traceability to ACB/TC IDs, AC
  sub-numbering. (Kids min-age looked durable but a QA probe showed the 2017 "min age 1" error does
  NOT fire on current QA → moved to re-probe-gated. Good example of the discipline working.)
- **RE-PROBE-GATED (confirm on QA, then encode):** occupational premium-change popup (our deferred
  AC27-29), IP S/U eligibility, employment-status, named-occupation→code, Loadings AC07 per-mille
  boundary, Business-SI AC13 greyed-out, and the client's historical defects (occ TPD U/M/A,
  commissions flexi-rate, bundling L400/farmers, multi-lives AC06/10/3 — many likely already fixed).
- **BA-CONFIRM:** commissions AC08, bundling 12.5/17.5% vs 15/20%, occ TPD-IC "required/requires".
- **BLOCKED:** clone (needs a submitted-app fixture), early-trauma SI + occ L400 (backend/PDF).

## PARKED (decisions for higher-ups)

Cloud/CI execution — `docs/cloud-execution.md`. **Gate:** can a cloud/VM egress IP be whitelisted for
QA? Recommended shape: self-hosted GitHub runner on a whitelisted VM + Pages dashboard (GH layer free;
cost = the VM). Corrected "always-on" → **auto start/stop VM**, ~US$10-25/mo nightly (or ~$6 spot, or
$0 on an existing in-network box). Workflow `.github/workflows/nightly-suite.yml` is drop-in but
INACTIVE until runner + `ACCOUNTS_JSON` secret + Pages exist. NOT to be actioned without sign-off.

## NEXT: continue finishing the remaining user stories

- Tracker: `apps/asteron-quote-apply/docs/user-story-tracker.md` (⬜ = not done). Remaining are
  largely the **Apply-flow / underwriting block** (Apply Now, Duty of Disclosure, Insurance History,
  Payment, Submit, Tele-Interview, URE/Underwriting, Doctor/Owner Details) + meta/non-functional docs.
- **Reachability reality:** most of these need the Apply flow past the payment gate — likely
  deferral-heavy until (a) the payment gate is bypassable in test, or (b) accounts get seeded data /
  a submitted-application fixture. Before generating, do the honest thing: **drive an application up
  to the payment gate and probe each screen** (Duty of Disclosure, Insurance History, Doctor/Owner
  Details, URE) — several may be reachable PRE-payment and fully testable now. Map the wall first.
- Process reminders: probe-before-assert; exhaustive per-AC (positive + negative + boundary-triple +
  value-level via recordCheck); deferrals get `test.fixme(true, reason)` + probe evidence; one test
  doc per spec; flip tracker; commit atomically; run via `tools/parallel-run.js` (10 accounts now).
