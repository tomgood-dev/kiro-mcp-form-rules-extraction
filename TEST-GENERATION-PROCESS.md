# Process — Generating Tests From a User Story (Fully Automated)

**Purpose:** A repeatable, instrumented procedure for turning a fresh user story into a runnable,
annotated, self-verified Playwright test suite — with minimal human intervention, and with every
decision logged for later refinement.

**Status:** First automated run in progress (ACB-2242 "Lump Sum Life Cover"). Being validated and
refined as we go — see the learnings file.

**Relationship to other docs:**
- Reuses the canonical AC format + stable internal IDs from `DELTA-UPDATE-PROCESS.md`
  (generation is the special case where there is no "before" — every AC is "added").
- Produces artifacts per `.kiro/steering/test-expansion-process.md` (report.md convention, AC
  annotation format, parallel/serial test structure, probe-safety rules, verify-before-reporting).
- Uses app facts from `.kiro/steering/project-context.md`.

---

## Inputs

- A user story markdown file in `apps/<app>/docs/user-stories/`.
- Live app credentials (`BASE_URL`, `ASTERON_LOGIN_EMAIL`, `ASTERON_LOGIN_PASSWORD`).

## Outputs

1. A spec file: `apps/<app>/tests/quote-screen/<slug>-v1.spec.js`
2. A test documentation matrix: `apps/<app>/docs/test-documentation/<slug>-v1.md`
3. A run report (auto-generated by the reporter): `test-runs/<slug>-v1/<timestamp>/report.md`
4. **A generation decision log:** `test-runs/<slug>-v1/generation-log-<timestamp>.md`
5. Appended lessons in the cumulative `TEST-GENERATION-LEARNINGS.md` (repo root).

---

## The procedure (what the agent does end to end)

### Step 1 — Extract ACs to canonical form
Parse the story into canonical AC records (internalId, sourceId, given/when/then, rawText,
sourceRef). Assign our own stable internal IDs. **Log:** every AC, and any parsing judgment calls
(e.g. an AC written as prose, a compound AC split into two).

### Step 2 — Classify each AC: testable-now / needs-decision / deferred
For each AC decide whether it's testable by driving the browser on the reachable screen. **Log:**
the classification and the reason for anything deferred (backend/STP, saved-quote state, etc.).
Never silently drop an AC.

### Step 3 — Probe the live app for selectors
Using the exploration server (`tools/server.js`) and the existing helpers
(`apps/asteron-quote-apply/helpers/`), discover the real DOM needed for each testable AC. **Reuse
existing helpers — do not rebuild them.** **Log:** for each AC, what was probed, what selector/
interaction was found, and any dead ends or ambiguity (this is the highest-value log data —
selector discovery is the predicted friction point).

### Step 4 — Generate the spec file
Write the spec following the mandatory conventions:
- Multiple `test()` blocks; independent checks in a `mode:'parallel'` describe; any
  state-mutating checks in a separate serial describe (see test-expansion-process.md).
- Each test has a `test.info().annotations.push({type:'acceptance-criteria', description: ...})`
  with verbatim AC text + steps to reproduce + expected + actual.
- Assertions written to the SPEC's expected value (so confirmed-failing ACs stay red until fixed).
**Log:** any AC where the intended behaviour was ambiguous and the agent had to make a call
(flag these — they may need author clarification, per the AC-wording feedback loop).

### Step 5 — Run against the live app
Run via `playwright.edge.config.js`. **Log:** pass/fail per test, runtime, any environment issues
(session conflicts, hangs).

### Step 6 — Self-verify surprising results
Apply the "verify before writing up" discipline (test-expansion-process.md): a surprising
result must reproduce on a second, minimal check before being trusted as a real finding — rule
out test-technique artifacts first. **Log:** anything re-verified and the outcome.

### Step 7 — Write the test-documentation matrix + finalise
Produce the `<slug>-v1.md` matrix (AC traceability). Confirm the reporter produced `report.md`.
**Log:** final summary — counts, deferrals, flagged ambiguities, and a short "what to improve
next time" note that also gets appended to `TEST-GENERATION-LEARNINGS.md`.

---

## The decision log (mandatory — this is the point of the exercise)

The agent maintains `generation-log-<timestamp>.md` throughout, capturing EVERY meaningful
decision — right and wrong — so the flow improves each run. At minimum, per step: what it decided,
why, what it was unsure about, and anything a human should review. A run that produces perfect
tests but a thin log is a failed run for our purposes.

## Autonomy

First runs execute **end to end without pausing**, logging verbosely; humans review the log +
report afterward. Fall back to gated pauses (after Step 1 and after Step 3) only if a run goes
badly.
